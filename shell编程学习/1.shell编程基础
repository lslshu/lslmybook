1.常见整数运算符

num1 -eq num2 比较1是否等于2，相等输出0
num1 -ne num2 比较1和2是否不相等，不想等相等输出0
num1 -gt num2 比较1是否大于2，大于输出0
num1 -lt num2 比较1是否小于2，小于输出0
num1 -ge num2 比较1是否大于等于2，大于等于输出0
num1 -le num2 比较1是否小于等于2，小于等于输出0

2.常用文件操作符

-a file 文件是否存在，如存在，输出0
-b file 文件是否存在，且为块文件，如存在，输出0
-c file 文件是否存在，且为字符文件，如存在，输出0
-d file 文件是否存在，且为目录，如存在，输出0
-e file 与a相同
-s file 文件长度是否大于0或者文件为非空文件，文件为非空文件，输出0
-f file 文件存在且为常规文件，输出0
-w file 文件是否存在且可写，是可写文件输出0
-L file 文件是否存在且为符号链接，是输出0
-u file 文件是否设置suid位，是输出0
-r file 文件是否可读，是输出0
-x file 文件是否可执行，是输出0
